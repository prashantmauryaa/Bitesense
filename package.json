{
  "name": "bitesense",
  "version": "2.0.0",
  "private": true,
  "description": "Bite Sense — MERN stack (MongoDB, Express, React, Node).",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm run build && npm start --prefix server",
    "migrate": "npm run migrate --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
